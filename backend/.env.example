# PrepSmart Environment Configuration
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Get your Claude API key from: https://console.anthropic.com/
# 3. Generate a secure Flask secret key:
#    python -c "import secrets; print(secrets.token_hex(32))"
# 4. Fill in the values below
#
# WARNING: NEVER commit .env to git! It contains sensitive credentials.

# ============================================================================
# REQUIRED: Claude API Configuration
# ============================================================================
# Get your API key from https://console.anthropic.com/
# The key should start with "sk-ant-api03-"
CLAUDE_API_KEY=your-claude-api-key-here

# ============================================================================
# REQUIRED: Flask Configuration
# ============================================================================
# Environment: development, production, or testing
FLASK_ENV=development

# Enable debug mode (set to False in production)
FLASK_DEBUG=True

# Secret key for session management (generate with: python -c "import secrets; print(secrets.token_hex(32))")
FLASK_SECRET_KEY=your-secret-key-here

# ============================================================================
# Database Configuration
# ============================================================================
# SQLite database location (relative to backend/ directory)
DATABASE_URL=sqlite:///prepsmart.db

# ============================================================================
# Agent Configuration
# ============================================================================
# Maximum time (seconds) for each agent to complete
AGENT_TIMEOUT=30

# Maximum number of concurrent AI tasks
MAX_CONCURRENT_TASKS=10

# ============================================================================
# Logging Configuration
# ============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# CORS Configuration (Development)
# ============================================================================
# Allow frontend from different port during local development
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# ============================================================================
# Production Settings (Azure Container Apps)
# ============================================================================
# Uncomment and modify these for production deployment:
# FLASK_ENV=production
# FLASK_DEBUG=False
# ALLOWED_ORIGINS=https://your-frontend-url.azurecontainerapps.io
# LOG_LEVEL=WARNING
